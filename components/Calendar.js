import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

export default function Calendar () {
    return (
        <SafeAreaView>
        <Text>Hola, this is your calendar</Text>
    </SafeAreaView>
    )
}